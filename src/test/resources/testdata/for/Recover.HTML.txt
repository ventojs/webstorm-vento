HtmlFile:a.vto(0,72)
  PsiElement(HTML_DOCUMENT)(0,0)
    PsiElement(XML_PROLOG)(0,0)
      <empty list>
  PsiElement(VENTO_OUTER_TOKEN)('{{ for number of [1,2,3] }}\n{{ "hello " + number }}\n{{  / }}\n{{ "hi!" }}')(0,72)