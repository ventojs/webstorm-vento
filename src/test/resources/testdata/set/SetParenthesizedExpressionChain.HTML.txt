HtmlFile:a.vto(0,86)
  PsiElement(HTML_DOCUMENT)(0,0)
    PsiElement(XML_PROLOG)(0,0)
      <empty list>
  PsiElement(VENTO_OUTER_TOKEN)('{{ set currentPathParts = (url || '/').split('/').filter((part) => part.length > 0) }}')(0,86)